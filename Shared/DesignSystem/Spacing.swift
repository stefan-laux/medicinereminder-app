//
//  Spacing.swift
//  MedicineReminder — DesignSystem
//
//  Consistent spacing scale used across layouts.
//

import CoreGraphics

public enum Spacing {
    /// 4 pt — tight inner padding.
    public static let xs: CGFloat = 4
    /// 8 pt — compact stacks.
    public static let sm: CGFloat = 8
    /// 12 pt — default element spacing.
    public static let md: CGFloat = 12
    /// 16 pt — card padding / standard gutters.
    public static let lg: CGFloat = 16
    /// 24 pt — section separation.
    public static let xl: CGFloat = 24
    /// 32 pt — large section / screen padding.
    public static let xxl: CGFloat = 32
}
